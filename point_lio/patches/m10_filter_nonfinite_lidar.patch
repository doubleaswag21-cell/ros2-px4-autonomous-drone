diff --git a/src/preprocess.cpp b/src/preprocess.cpp
index da017ac..65971ad 100644
--- a/src/preprocess.cpp
+++ b/src/preprocess.cpp
@@ -1,3 +1,4 @@
+#include <cmath>
 #include "preprocess.h"
 
 #define RETURN0     0x00
@@ -215,6 +216,15 @@ void Preprocess::velodyne_handler(const sensor_msgs::msg::PointCloud2::SharedPtr
         added_pt.y = pl_orig.points[i].y;
         added_pt.z = pl_orig.points[i].z;
 
+        // Gazebo can represent LiDAR no-return rays as +/-Inf.
+        // Reject all non-finite XYZ points before they enter Point-LIO.
+        if (!std::isfinite(added_pt.x) ||
+            !std::isfinite(added_pt.y) ||
+            !std::isfinite(added_pt.z))
+        {
+            continue;
+        }
+
         added_pt.intensity = pl_orig.points[i].intensity;
         added_pt.curvature = pl_orig.points[i].time * time_unit_scale;  // curvature unit: ms // cout<<added_pt.curvature<<endl;
         if (!given_offset_time) {
